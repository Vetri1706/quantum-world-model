import type { ResolvedExperiment } from "../models/ResolvedExperiment.js";

/** A fully dereferenced experiment graph ready for a consumer subsystem. */
export type ExperimentGraph = ResolvedExperiment;
