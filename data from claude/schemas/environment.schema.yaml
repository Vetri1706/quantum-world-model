# Structure only — no values filled in.

id:              # unique string
name:            # string
pressure:        # value + unit
gravity:         # value + unit
temperature:     # value + unit
atmosphere:      # string or null
allows:          # list of strings — experiment/element types this environment permits
blocks:          # list of strings — experiment/element types this environment forbids
